병합 취소 후 다시 병합

병합 취소
$ git merge--abort

다시 병합
$ git merge feat/list

직접 수정 후 저장
-충돌 표시 모두 거

추가,커밋 다시 
$ git commit-am 'Resolve confilict, main'

3-way 충돌 발생
$ git merge hotfix
충돌한 파일을 인지하고 파일 수정
$ code file
수정 후 다시 add,commit
$ git commit -am'msg'
충돌 이후 병합 취소
$ git merge --abort
